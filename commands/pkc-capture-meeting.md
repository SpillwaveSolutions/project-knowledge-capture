---
name: pkc-capture-meeting
description: Capture meeting notes → Meeting + DecisionRecords (+ optional tickets).
---

Run the **pkc-capture-meeting** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/pkc-capture-meeting/SKILL.md` completely. Prefer deterministic helpers under `${CLAUDE_PLUGIN_ROOT}/scripts/` when applicable. Write valid OKF Markdown with absolute links and update catalogs/log.
