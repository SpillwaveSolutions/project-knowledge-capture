---
name: pkc-init
description: Scaffold a PKC knowledge bundle with all catalogs.
---

Run the **pkc-init** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/pkc-init/SKILL.md` completely. Prefer deterministic helpers under `${CLAUDE_PLUGIN_ROOT}/scripts/` when applicable. Write valid OKF Markdown with absolute links and update catalogs/log.
