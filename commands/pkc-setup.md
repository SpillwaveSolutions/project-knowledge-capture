---
name: pkc-setup
description: Check PKC toolchain (ripgrep, Python, SQLite FTS5) and optionally install ripgrep after the user consents.
---

Follow `${CLAUDE_PLUGIN_ROOT}/skills/pkc-setup/SKILL.md`.

User request: `$ARGUMENTS`
